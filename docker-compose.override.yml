services:
  
  mssql:
    container_name: mssql
    restart: always
    ports:
      - "1433:1433"
    environment:
      - ACCEPT_EULA=Y
      - MSSQL_SA_PASSWORD=123456789Abc*
      - MSSQL_PID=Evaluation
    networks:
      - app-network
  
  elasticsearch:
    container_name: elasticsearch
    restart: always
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
    volumes:
      - elastic_search_volume:/usr/share/elasticsearch/data
    ports:
      - "9200:9200"
    networks:
      - app-network
  
  kibana:
    container_name: kibana
    restart: always
    ports:
      - "5601:5601"
    depends_on:
      - elasticsearch
    networks:
      - app-network

  rabbitmq:
    container_name: rabbitmq
    restart: always
    ports:
      - "5672:5672"
      - "15672:15672"
    volumes:
      - rabbitmq_volume:/var/lib/rabbitmq
    networks:
      - app-network

  redis:
    container_name: redis
    restart: always
    ports:
      - "6379:6379"
    networks:
      - app-network

  authservice.api:
    container_name: authserviceapi
    restart: always
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_HTTP_PORTS=80
      - ConnectionStrings:SqlServer=Server=mssql,1433;Database=AuthDB;User Id=sa;Password=123456789Abc*;TrustServerCertificate=True;
      - RabbitMQOptions:Host=rabbitmq
      - RabbitMQOptions:VirtualHost=/
      - RabbitMQOptions:Password=guest
      - RabbitMQOptions:UserName=guest
      - RabbitMQOptions:LogExchangeName=LogExchange
      - AuthOptions:Issuer=http://authservice.api
      - AuthOptions:SecurityKey=top_secret_security_key_top_secret_security_key_top_secret_security_key_top_secret_security_key
      - TokenOptions:Issuer=http://authservice.api
      - TokenOptions:AccessTokenValidtyPeriod=15
      - TokenOptions:RefreshTokenValidtyPeriod=30
      - TokenOptions:SecurityKey=top_secret_security_key_top_secret_security_key_top_secret_security_key_top_secret_security_key 
    ports:
      - "80"
    depends_on:
      - mssql
      - rabbitmq
    volumes:
      - ${APPDATA}/Microsoft/UserSecrets:/home/app/.microsoft/usersecrets:ro
      - ${APPDATA}/Microsoft/UserSecrets:/root/.microsoft/usersecrets:ro
    networks:
      - app-network

  productservice.api:
    container_name: productserviceapi
    restart: always
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_HTTP_PORTS=80
      - ConnectionStrings:SqlServer=Server=mssql,1433;Database=ProductDB;User Id=sa;Password=123456789Abc*;TrustServerCertificate=True;
      - RabbitMQOptions:Host=rabbitmq
      - RabbitMQOptions:VirtualHost=/
      - RabbitMQOptions:Password=guest
      - RabbitMQOptions:UserName=guest
      - RabbitMQOptions:LogExchangeName=LogExchange
      - AuthOptions:Issuer=http://authservice.api
      - AuthOptions:SecurityKey=top_secret_security_key_top_secret_security_key_top_secret_security_key_top_secret_security_key
      - Redis:Host=redis:6379
    ports:
      - "80"
    depends_on:
      - mssql
      - rabbitmq
      - redis
    volumes:
      - ${APPDATA}/Microsoft/UserSecrets:/home/app/.microsoft/usersecrets:ro
      - ${APPDATA}/Microsoft/UserSecrets:/root/.microsoft/usersecrets:ro
    networks:
      - app-network

  logservice.api:
    container_name: logserviceapi
    restart: always
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_HTTP_PORTS=80
      - ElasticSearchOptions:Host=http://elasticsearch:9200/
    ports:
      - "80"
    depends_on:
      - elasticsearch
    volumes:
      - ${APPDATA}/Microsoft/UserSecrets:/home/app/.microsoft/usersecrets:ro
      - ${APPDATA}/Microsoft/UserSecrets:/root/.microsoft/usersecrets:ro
    networks:
      - app-network

  logservice.worker:
    container_name: logserviceworker
    restart: always
    environment:
      - DOTNET_ENVIRONMENT=Development
      - ElasticSearchOptions:Host=http://elasticsearch:9200/
      - RabbitMQOptions:Host=rabbitmq
      - RabbitMQOptions:VirtualHost=/
      - RabbitMQOptions:Password=guest
      - RabbitMQOptions:UserName=guest
      - RabbitMQOptions:LogExchangeName=LogExchange
    depends_on:
      - rabbitmq
      - elasticsearch
    volumes:
      - ${APPDATA}/Microsoft/UserSecrets:/home/app/.microsoft/usersecrets:ro
      - ${APPDATA}/Microsoft/UserSecrets:/root/.microsoft/usersecrets:ro
    networks:
      - app-network

  gateway.api:
    container_name: gatewayapi
    restart: always
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_HTTP_PORTS=80
      - RabbitMQOptions:Host=rabbitmq
      - RabbitMQOptions:VirtualHost=/
      - RabbitMQOptions:Password=guest
      - RabbitMQOptions:UserName=guest
      - RabbitMQOptions:LogExchangeName=LogExchange
      - AuthOptions:Issuer=http://authservice.api
      - AuthOptions:SecurityKey=top_secret_security_key_top_secret_security_key_top_secret_security_key_top_secret_security_key

      - ReverseProxy:Clusters:user.cluster:Destinations:destination:Address=http://authservice.api
      - ReverseProxy:Clusters:product.cluster:Destinations:destination:Address=http://productservice.api
      - ReverseProxy:Clusters:log.cluster:Destinations:destination:Address=http://logservice.api
    ports:
      - "5001:80"
    depends_on:
      - rabbitmq
      - authservice.api
      - productservice.api
      - logservice.api
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
